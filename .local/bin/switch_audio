#!/bin/sh
wpctl status | grep -A 10 "Sinks:" | grep -E '^[[:space:]]*│[[:space:]]*(\*|[[:space:]]+)[0-9]+\..*\[vol:' | sed -E 's/^[[:space:]]*│[[:space:]]*(\*|[[:space:]]+)([0-9]+)\. (.*) \[vol:.*/\2 \3/' | wofi --dmenu -p "Select Audio Sink" | awk '{print $1}' | xargs wpctl set-default
